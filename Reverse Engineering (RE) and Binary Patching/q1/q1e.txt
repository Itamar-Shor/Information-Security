In this function We want to change only the return value and not the printed output. So I cahnged only the main function and not 'validate'
The main function checks the retrun value of 'validate' and accordingly puts 0/1 in EAX. I changed the command which puts 1 in EAX to puts 0 in EAX.